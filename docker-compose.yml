version: '3.8'

services:
  web:
    build: .
    command: gunicorn --bind 0.0.0.0:8000 Registros.wsgi:application
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    # env_file:
    #   - .env # Uncomment if you have a .env file
